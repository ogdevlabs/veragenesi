{
  "name": "vera-genesi-api",
  "version": "0.1.0",
  "description": "VeraGenesi - Emotional Health App (Backend API)",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "predev": "node scripts/ensure-docker.js",
    "dev": "nodemon src/index.js",
    "stop": "pkill -f 'nodemon src/index.js' || true",
    "stop-all": "cd .. && ./stop-all.sh",
    "validate": "node scripts/validate-seed.js",
    "test": "jest --coverage",
    "lint": "eslint .",
    "format": "prettier --write ."
  },
  "dependencies": {
    "express": "^4.18.2",
    "dotenv": "^16.0.0",
    "cors": "^2.8.5",
    "pg": "^8.11.0",
    "bcrypt": "^5.1.1",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.0",
    "jest": "^29.0.0",
    "supertest": "^6.3.0",
    "eslint": "^8.0.0",
    "prettier": "^3.0.0"
  }
}
